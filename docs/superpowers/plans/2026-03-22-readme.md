# README Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为仓库补一份部署优先型 `README.md`，让部署者可以直接完成安装、配置与联调。

**Architecture:** 仅新增一个根目录 `README.md`，内容严格基于当前仓库已存在的部署资料与目录结构。README 负责首页导航，复杂细节继续指向 `SETUP.md`、`docs/design.md` 与 `docs/plan.md`。

**Tech Stack:** Markdown、Git、现有仓库文档

---

## File Map

- Create: `README.md`
  Purpose: 提供项目简介、核心能力、快速部署、配置要点、启动联调、目录结构与补充文档入口。
- Reference: `SETUP.md`
  Purpose: 提供部署命令和网关重启说明。
- Reference: `docs/design.md`
  Purpose: 提供多 Agent 架构和目录职责背景。

### Task 1: Create Deployment-Focused README

**Files:**
- Create: `README.md`
- Modify: none
- Test: none

- [ ] **Step 1: Draft the README structure**

Write these sections in order:
- `# 荀彧（xunyu）辅导作业系统`
- 项目简介
- 核心能力
- 快速部署
- 网关配置要点
- 启动与联调
- 目录结构
- 补充文档

- [ ] **Step 2: Fill the deployment commands with exact repository paths**

Include commands based only on existing files:

```bash
git clone git@github.com:codenot/xunyu.git
cd xunyu
./install.sh
openclaw gateway restart
```

Also mention:
- `openclaw.json`
- `channels.qqbot.agentId`
- `agents.list`
- `agents/coordinator`
- `agents/marker`
- `agents/inspector`
- `scripts/`
- 将仓库根目录 `openclaw.json` 合并或覆盖到 `/home/ubuntu/.openclaw/openclaw.json`

- [ ] **Step 3: Make the gateway validation checks explicit**

Require the README to tell deployers to确认以下三点：
- `channels.qqbot.agentId` 指向 `coordinator`
- `agents.list` 已注册三个 Agent
- 本地目录结构包含 `agents/coordinator`、`agents/marker`、`agents/inspector` 和 `scripts/`
- 在重启网关前，必须先让仓库根目录 `openclaw.json` 生效到 `/home/ubuntu/.openclaw/openclaw.json`

- [ ] **Step 4: Keep the homepage concise and operational**

Ensure the README:
- prioritizes deployment over architecture detail
- does not duplicate `docs/design.md`
- links to `SETUP.md`, `docs/design.md`, and `docs/plan.md`

- [ ] **Step 5: Verify the rendered content by reading the file back**

Run: `sed -n '1,240p' README.md`
Expected: sections are present, commands are valid, wording is deployment-oriented, and the file explicitly mentions:
- `channels.qqbot.agentId`
- `agents.list`
- `agents/coordinator` / `agents/marker` / `agents/inspector`
- `/home/ubuntu/.openclaw/openclaw.json`
- 通过“帮我批改作业”进行联调

- [ ] **Step 6: Commit the README**

```bash
git add README.md
git commit -m "docs: add deployment-focused readme"
```
